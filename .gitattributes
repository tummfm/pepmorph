# Model binaries: keep in Git LFS
artifacts/models/**/*.pt filter=lfs diff=lfs merge=lfs -text

# Preview trajectories: store in normal Git, not LFS
*.xtc -filter -diff -merge -text

# GROMACS XVG analysis files: store as normal Git text files
*.xvg text eol=lf
